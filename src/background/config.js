const defaultConfig = {
    enabled: true,
    whitelist: [],
    customBlacklist: [],
};

/** TODO
 * - Load config (using Chrome API)
 * - Save config (using Chrome API)
 * - Convert old config to new
 */

const getConfig = () => {
    // TODO  
};

const saveConfig = () => {
    // TODO
};